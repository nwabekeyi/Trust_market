{
  "name": "trust_market",
  "version": "1.0.0",
  "description": "A marketplace application",
  "scripts": {
    "start": "cd controller && npm install; cd .../View && npm install && vite build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.2",
    "crypto": "^1.0.1"
  }
}
