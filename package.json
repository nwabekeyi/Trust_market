{
  "name": "trust_market",
  "version": "1.0.0",
  "description": "A marketplace application",
  "scripts": {
    "build": "cd controller && npm install && cd ../View && npm install && npm run build",
    "start": "cd controller && node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.2",
    "crypto": "^1.0.1",
    "framer-motion": "^11.2.9"
  }
}
